# Dad Joke Generator 😂

A modern, minimal Dad Joke Generator web app built using **HTML, CSS, and Vanilla JavaScript**.  
The app fetches jokes asynchronously from a public API and displays them in a clean, readable UI.

---

## ✨ Features

- Fetches random dad jokes using the **Fetch API**
- Clean and modern UI with a glassmorphism-style joke card
- Full-page background image with gradient overlay
- Button to load a new joke instantly
- Responsive and beginner-friendly codebase

---

## 🛠️ Tech Stack

- **HTML5** – Structure  
- **CSS3** – Styling, Flexbox, background effects  
- **JavaScript (ES6+)** – Fetch API, DOM manipulation  

---

## 🔌 API Used

- [icanhazdadjoke](https://icanhazdadjoke.com/)
  - Jokes are fetched in JSON format using custom request headers.

---

## 🚀 How It Works

1. The page loads with a default joke.
2. Clicking the **“Another one →”** button triggers a new API request.
3. The joke text updates dynamically without reloading the page.

---

## 📚 What I Learned

- Making asynchronous API requests using `fetch`
- Handling JSON responses
- DOM selection and event handling
- Flexbox-based layout and modern UI styling
- Debugging CSS layout and background behavior

